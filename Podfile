# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PCCatalog' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PCCatalog
  pod 'Alamofire', '~> 5.5'
  pod 'PromiseKit', '~> 6.8'

  target 'PCCatalogTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PCCatalogUITests' do
    # Pods for testing
  end

end
